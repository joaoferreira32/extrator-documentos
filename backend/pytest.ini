[pytest]
markers =
    e2e: testes de interface no navegador (Playwright); so rodam com --e2e (ver requirements-dev.txt)
    ooxml_sdk: validacao do .xlsx com o Open XML SDK da Microsoft; so roda com --ooxml-sdk (Windows)
